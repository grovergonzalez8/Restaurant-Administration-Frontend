import { HttpClient, HttpParams } from '@angular/common/http'; import { Injectable } from '@angular/core'; import { environment } from '../../../environments/environment'; import { InventoryReport, SalesReport, TopProduct } from '../../core/models/report.model';
@Injectable({providedIn:'root'}) export class ReportsService { private readonly url=`${environment.apiUrl}/reports`; constructor(private http:HttpClient){} private params(from?:string,to?:string){let params=new HttpParams();if(from)params=params.set('from',from);if(to)params=params.set('to',to);return {params};} sales(from?:string,to?:string){return this.http.get<SalesReport>(`${this.url}/sales`,this.params(from,to));} topProducts(from?:string,to?:string){return this.http.get<TopProduct[]>(`${this.url}/top-products`,this.params(from,to));} inventory(from?:string,to?:string){return this.http.get<InventoryReport>(`${this.url}/inventory`,this.params(from,to));} }
