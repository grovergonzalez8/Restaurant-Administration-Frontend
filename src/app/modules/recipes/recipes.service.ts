import { HttpClient } from '@angular/common/http'; import { Injectable } from '@angular/core'; import { environment } from '../../../environments/environment'; import { RecipeItem } from '../../core/models/recipe.model';
@Injectable({providedIn:'root'}) export class RecipesService { private readonly url=`${environment.apiUrl}/recipes`; constructor(private http:HttpClient){} all(){return this.http.get<RecipeItem[]>(this.url);} create(menuItemId:string,inventoryItemId:string,quantity:number){return this.http.post<RecipeItem>(this.url,{menuItemId,inventoryItemId,quantity});} remove(id:string){return this.http.delete<void>(`${this.url}/${id}`);} }
